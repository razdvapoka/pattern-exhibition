import React from "react"

const CuratorsSection = ({ setIsVideoNavVisible, title, text, url, urlText, ...rest }) => (
  <div className="h-screen" {...rest}>
    curators section
  </div>
)

export default CuratorsSection
