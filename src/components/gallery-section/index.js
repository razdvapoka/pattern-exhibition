import React from "react"

const GallerySection = ({ setIsVideoNavVisible, title, text, url, urlText, ...rest }) => (
  <div className="h-screen" {...rest}>
    gallery section
  </div>
)

export default GallerySection
