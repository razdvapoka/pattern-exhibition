import React from "react"

const ScheduleSection = ({ setIsVideoNavVisible, title, text, url, urlText, ...rest }) => (
  <div className="h-screen" {...rest}>
    schedule section
  </div>
)

export default ScheduleSection
