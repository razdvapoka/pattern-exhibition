import React from "react"

const RobotSection = ({ setIsVideoNavVisible, title, text, url, urlText, ...rest }) => (
  <div id="robot" className="h-screen" {...rest}>
    robot section
  </div>
)

export default RobotSection
