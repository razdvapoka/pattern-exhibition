import React from "react"

const SubscribeSection = ({ setIsVideoNavVisible, title, text, url, urlText, ...rest }) => (
  <div className="h-screen" {...rest}>
    subscribe section
  </div>
)

export default SubscribeSection
